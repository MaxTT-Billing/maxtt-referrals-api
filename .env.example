# Referrals API — env example
PORT=11000

# Allow your tools domains (comma-separated, NO trailing slash)
CORS_ALLOWED_ORIGINS=https://maxtt-billing-tools.onrender.com, https://tools.treadstonesolutions.co.in

# HMAC shared secret (MUST match Billing API)
REF_SIGNING_KEY=TS!MAXTT-2025

# Toggle whole service (true/false)
REFERRALS_ENABLED=true

# Postgres connection string (Render provides this)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

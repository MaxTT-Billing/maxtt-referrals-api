# MaxTT Referrals API — Example Environment File

# --- Server ---
PORT=11000

# --- CORS ---
# Comma-separated origins allowed to call this API
CORS_ALLOWED_ORIGINS=https://maxtt-billing-tools.onrender.com,https://tools.maxtt.in

# --- Security ---
# HMAC signing secret (TEMP dev value acceptable; MUST upgrade before production)
REF_SIGNING_KEY=TS!MAXTT-2025

# --- Feature flags ---
# Toggle referrals endpoints on/off (true/false)
REFERRALS_ENABLED=true

# Optional timeout for billing-side requests (not used by this server but kept for parity)
REFERRALS_TIMEOUT_MS=5000

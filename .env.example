NODE_ENV=production
PORT=8080
DATABASE_URL=postgres://user:pass@host:5432/maxtt_referrals

# Raw keys only in env (DB stores hashes at runtime)
WRITER_API_KEY=change-me-writer
ADMIN_API_KEY=change-me-admin
SA_API_KEY=change-me-sa

# Comma-separated origins (no trailing slashes)
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://maxtt-seal-earn-web.vercel.app
